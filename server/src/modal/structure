import * as mongoose from "mongoose";
import { Environment } from "config/config";

const environment: Environment = <any>process.env;

/** Typescript Modal  */

export interface  {

}

/** Mongoose Schema and Modal */

const Schema = new mongoose.Schema< & mongoose.Document>({

});

export const Schema = new mongoose.Schema< & mongoose.Document>({

});

/**
 * Schema @Methods
 */

export const Modal  = mongoose.model("", Schema);
